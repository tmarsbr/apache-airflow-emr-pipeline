# Exemplo de arquivo de credenciais AWS
# Copie este arquivo para aws_credentials e preencha com suas credenciais

[default]
aws_access_key_id = SEU_ACCESS_KEY_AQUI
aws_secret_access_key = SEU_SECRET_KEY_AQUI
region = us-east-1

# IMPORTANTE: Nunca commite o arquivo aws_credentials real!
# Este arquivo está incluído no .gitignore por segurança